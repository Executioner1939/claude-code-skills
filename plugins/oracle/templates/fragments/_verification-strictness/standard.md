### Strictness: standard

Run the full three-tier cascade. Stop at the first authoritative tier.
A `verified` verdict requires at least one citation in the configured
format. A `partially verified` verdict is allowed when one of several
sub-claims is unverifiable; explicitly list which part is which.
A `refuted` or `unverified` verdict blocks any downstream action that
depends on the claim.
