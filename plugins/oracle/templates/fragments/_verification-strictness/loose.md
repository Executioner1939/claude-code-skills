### Strictness: loose

Best-effort verification. Tier 1 may be skipped for non-version claims;
Tier 2 results are accepted at face value when the URL is reachable.
A `partially verified` verdict is acceptable when the cascade returns
suggestive but non-authoritative evidence. Do not block downstream
work on a single unverified claim.
