{
  "$schema": "../preset.schema.json",
  "name": "image-no-alt",
  "description": "<img> elements without an alt attribute. WCAG 1.1.1 — every image needs alt text (or alt=\"\" for decorative). Catches both raw <img> and `next/image`-style components when the imported name resolves to an `Image` element.",
  "filter": {
    "anyOf": [
      {
        "allOf": [
          { "tag": "img" },
          { "not": { "attr": "alt" } }
        ],
        "ruleId": "img-no-alt",
        "reason": "<img> requires `alt` (use alt=\"\" for decorative images)."
      }
    ]
  }
}
