{
  "description": "Auto-refresh the design-system component inventory when component files are edited. Opt-in: only runs when the project has a .anvil/ directory or .anvil.yml at its root. Debounced to 30s.",
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "bash ${CLAUDE_PLUGIN_ROOT}/hooks/refresh-inventory.sh",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
